# Быстрая справка: Поля для POST запросов

## 📋 Категория (Category)

| Поле | Тип | Обязательное | Ограничения |
|------|-----|--------------|-------------|
| `name` | string | ✅ Да | max 100 символов |
| `icon` | string | ❌ Нет | max 100 символов |

**Пример:**
```json
{
  "name": "Технологии",
  "icon": "tech-icon"
}
```

---

## 👤 Пользователь (User)

| Поле | Тип | Обязательное | Ограничения |
|------|-----|--------------|-------------|
| `email` | string | ✅ Да | email формат, max 256 символов |
| `passwordHash` | string | ✅ Да | min 6 символов |
| `firstName` | string | ✅ Да | max 100 символов |
| `lastName` | string | ❌ Нет | max 100 символов |
| `defaultPlaybackSpeed` | double | ❌ Нет | 0 < value <= 3.0 (по умолчанию 1.0) |

**Пример:**
```json
{
  "email": "user@example.com",
  "passwordHash": "mypassword123",
  "firstName": "Иван",
  "lastName": "Иванов",
  "defaultPlaybackSpeed": 1.0
}
```

---

## 🎙️ Подкаст (Podcast)

| Поле | Тип | Обязательное | Ограничения |
|------|-----|--------------|-------------|
| `name` | string | ✅ Да | max 255 символов |
| `rssFeedUrl` | string | ✅ Да | валидный URL, max 500 символов |
| `description` | string | ❌ Нет | max 2000 символов |
| `coverImageUrl` | string | ❌ Нет | валидный URL, max 500 символов |
| `categoryId` | int? | ❌ Нет | должен существовать в БД |
| `language` | string | ❌ Нет | max 10 символов (по умолчанию "ru") |
| `isFavorite` | bool | ❌ Нет | по умолчанию false |
| `lastUpdatedAt` | DateTime | ❌ Нет | формат ISO 8601 |

**Пример:**
```json
{
  "name": "Подкаст о технологиях",
  "description": "Еженедельный подкаст",
  "rssFeedUrl": "https://example.com/feed.xml",
  "coverImageUrl": "https://example.com/cover.jpg",
  "categoryId": 1,
  "language": "ru",
  "isFavorite": false,
  "lastUpdatedAt": "2024-01-15T10:00:00Z"
}
```

---

## 📻 Эпизод (Episode)

| Поле | Тип | Обязательное | Ограничения |
|------|-----|--------------|-------------|
| `podcastId` | int | ✅ Да | > 0, должен существовать в БД |
| `title` | string | ✅ Да | max 500 символов |
| `audioFileUrl` | string | ✅ Да | валидный URL, max 1000 символов |
| `publishedAt` | DateTime | ✅ Да | формат ISO 8601 |
| `description` | string | ❌ Нет | max 5000 символов |
| `durationInSeconds` | int | ❌ Нет | >= 0 (по умолчанию 0) |
| `fileSizeInBytes` | long? | ❌ Нет | >= 0 |
| `progressInSeconds` | int | ❌ Нет | >= 0 (по умолчанию 0) |
| `isDownloaded` | bool | ❌ Нет | по умолчанию false |

**Пример:**
```json
{
  "podcastId": 1,
  "title": "Эпизод 1: Введение",
  "description": "Описание эпизода",
  "publishedAt": "2024-01-15T10:00:00Z",
  "durationInSeconds": 3600,
  "audioFileUrl": "https://example.com/audio/ep1.mp3",
  "fileSizeInBytes": 52428800,
  "progressInSeconds": 0,
  "isDownloaded": false
}
```

---

## 📝 Плейлист (Playlist)

| Поле | Тип | Обязательное | Ограничения |
|------|-----|--------------|-------------|
| `name` | string | ✅ Да | max 255 символов |
| `ownerId` | int | ✅ Да | > 0, должен существовать в БД |
| `description` | string | ❌ Нет | max 1000 символов |

**Пример:**
```json
{
  "name": "Мои любимые эпизоды",
  "description": "Подборка лучших эпизодов",
  "ownerId": 1
}
```

---

## ➕ Добавление эпизода в плейлист

**POST /api/playlists/{id}/episodes/{episodeId}**

Без тела запроса! Используйте только URL параметры:
- `id` - ID плейлиста
- `episodeId` - ID эпизода

**Пример:** `POST /api/playlists/1/episodes/1`

---

## ⚠️ Важно:

1. **Не передавайте:** `id`, `createdAt`, `updatedAt` - устанавливаются автоматически
2. **Не передавайте:** навигационные свойства (`category`, `podcast`, `owner`, `episodes` и т.д.)
3. **Формат даты:** ISO 8601 - `"2024-01-15T10:00:00Z"`
4. **URL:** должны быть валидными абсолютными URL (`http://` или `https://`)



