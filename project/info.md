## 📦 **СУЩНОСТИ ДЛЯ CRUD ОПЕРАЦИЙ**

### **1. Подкаст (Podcast)**

Сущность для хранения информации о подкасте. Поля: уникальный идентификатор, название, описание, ссылка на RSS-ленту, ссылка на обложку, категория, язык, дата последнего обновления, флаг "избранное".

### **2. Эпизод (Episode)**

Привязан к подкасту. Поля: идентификатор, ссылка на родительский подкаст, название эпизода, описание, дата публикации, длительность в секундах, прямая ссылка на аудиофайл, размер файла, прогресс прослушивания (сколько секунд прослушано), статус "скачан/не скачан".

### **3. Пользователь (User)**

Если делаем систему аккаунтов. Поля: идентификатор, email, хэш пароля, имя, настройки пользователя (например, скорость воспроизведения по умолчанию).

### **4. Плейлист (Playlist)**

Пользовательские коллекции эпизодов. Поля: идентификатор, название, описание, идентификатор владельца, список эпизодов в плейлисте.

### **5. Категория (Category)**

Для классификации подкастов. Поля: идентификатор, название, иконка.

## 🔄 **CRUD ОПЕРАЦИИ ДЛЯ КАЖДОЙ СУЩНОСТИ**

### **Для Подкастов:**

- **CREATE** — добавить новый подкаст по RSS-ссылке (система сама парсит ленту)
- **READ** — получить список всех подкастов, получить один подкаст по ID, поиск по названию
- **UPDATE** — обновить информацию о подкасте (например, пометить как избранное), принудительно обновить эпизоды из RSS
- **DELETE** — удалить подкаст и все связанные эпизоды (каскадно или с подтверждением)

### **Для Эпизодов:**

- **CREATE** — обычно создается автоматически при добавлении подкаста или обновлении RSS
- **READ** — получить все эпизоды подкаста, получить последние эпизоды, поиск по названию эпизода
- **UPDATE** — обновить прогресс прослушивания, пометить как прослушанное, добавить заметки к эпизоду
- **DELETE** — удалить эпизод (например, чтобы освободить место)

## 🌐 **ОСНОВНЫЕ КОНЕЧНЫЕ ТОЧКИ (ENDPOINTS)**

### **1. Управление подписками**

- `GET /api/podcasts` — список всех подкастов с пагинацией
- `GET /api/podcasts/{id}` — детальная информация о подкасте
- `POST /api/podcasts` — добавить новый подкаст по RSS URL
- `PUT /api/podcasts/{id}` — обновить информацию
- `DELETE /api/podcasts/{id}` — отписаться от подкаста
- `GET /api/podcasts/{id}/episodes` — все эпизоды конкретного подкаста

### **2. Управление эпизодами**

- `GET /api/episodes/recent` — последние выпуски из всех подписок
- `GET /api/episodes/unplayed` — непрослушанные эпизоды
- `PUT /api/episodes/{id}/progress` — обновить прогресс прослушивания
- `POST /api/episodes/{id}/download` — запустить скачивание эпизода
- `GET /api/episodes/downloaded` — все скачанные эпизоды

### **3. Плейлисты и очередь**

- `GET /api/playlists` — все плейлисты пользователя
- `POST /api/playlists` — создать новый плейлист
- `POST /api/playlists/{id}/episodes` — добавить эпизод в плейлист
- `GET /api/queue` — текущая очередь воспроизведения

### **4. Поиск и рекомендации**

- `GET /api/search?q={query}` — поиск по подкастам и эпизодам
- `GET /api/recommendations` — рекомендованные подкасты (на основе прослушанного)

## 🛠 **TECH STACK ДЛЯ API**

### **Основные технологии:**

- **ASP.NET Core Web API** — основа
- **Entity Framework Core** — для работы с базой данных
- **SQLite** (для разработки) или **PostgreSQL** (для продакшена) — база данных
- **Swagger/OpenAPI** — автоматическая документация API
- **JWT Authentication** — если делаем систему пользователей
- **Hangfire** или **BackgroundService** — для фоновых задач (обновление RSS)

### **Дополнительные компоненты:**

- **AutoMapper** — для преобразования между сущностями и DTO
- **FluentValidation** — для валидации входных данных
- **Serilog** или **NLog** — для логирования
- **Redis** (опционально) — для кэширования популярных запросов
